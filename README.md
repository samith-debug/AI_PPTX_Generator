# PPTX Generator - Groq Edition

This project uses Groq (Llama-3.1) as the default free AI backend.

Setup:
1. python3 -m venv venv
2. source venv/bin/activate
3. pip install -r requirements.txt
4. place theme0.pptx in project root (optional)
5. run python3 ui.py
